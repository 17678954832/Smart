<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.muhan.smart.dao.MallOrderItemMapper">
    <!--共用部分-->
    <sql id="Basic_Cloum_list">
        id, user_id, order_no, product_id, product_name, product_image, current_unit_price, quantity, total_price, create_time, update_time
    </sql>

    <!--查询语句-->
    <select id="queryById" resultType="com.muhan.smart.pojo.MallOrderItem">
        select
            <include refid="Basic_Cloum_list" />
        from mall_order_item
        where id = #{id}
    </select>

</mapper>